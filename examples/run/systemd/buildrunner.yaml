# Description: This example demonstrates how to use systemd for a run step.
steps:
  enable-systemd-step:
    run:
      image: centos:7
      cmd: echo Hello World
      systemd: True
  disable-systemd-step:
    run:
      image: centos:7
      cmd: echo Hello World
      systemd: False